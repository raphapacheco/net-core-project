using System;
using System.Collections.Generic;
using System.Text;

namespace BackEnd.NetCore.Usuario.Commands.Handlers
{
    public class UsuarioCommandHandler
    {
        //public  async Task<TResponse> Handle(TInserirCommand command, CancellationToken cancellationToken)
        //{
        //    if (!command.Valido(out var resultadoValidacaoCommand))
        //    {
        //        throw new ValidacaoException($"Command inválido:{ string.Join("\n\n", resultadoValidacaoCommand)}");
        //    }

        //    var entidade = _parser.ConverterParaEntidade(command);

        //    await _servico.InserirAsync(entidade);

        //    return _parser.ConverterParaResponse(entidade);
        //}
    }
}
